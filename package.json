{
  "name": "stack-duel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "www": "node scripts/sync-www.js",
    "cap:sync": "node scripts/sync-www.js && npx cap sync android",
    "cap:open": "npx cap open android",
    "android:debug": "node scripts/sync-www.js && npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:bundle": "node scripts/sync-www.js && npx cap sync android && cd android && ./gradlew bundleRelease",
    "build": "node scripts/sync-www.js",
    "sync": "node scripts/sync-www.js && npx cap sync android"
  },
  "dependencies": {
    "@capacitor-community/admob": "^7.0.3",
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "esbuild": "^0.25.0"
  },
  "description": "Stack Duel mobile wrap"
}
