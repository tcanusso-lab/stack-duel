/**
 * AdMob IDs — Google official TEST units until Tomás pastes real ones.
 * Swap APP_ID + REWARDED_UNIT_ID, then rebuild Android.
 * AndroidManifest meta-data uses APP_ID (see docs/android.md).
 */
window.STACK_DUEL_ADS = {
  // Google sample App ID
  APP_ID: "ca-app-pub-3940256099942544~3347511713",
  // Google sample Rewarded unit
  REWARDED_UNIT_ID: "ca-app-pub-3940256099942544/5224354917",
  MOCK_DELAY_MS: 1500,
};
